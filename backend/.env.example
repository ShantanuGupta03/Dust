# Backend environment configuration
PORT=4000
ALLOWED_ORIGINS=http://localhost:3000

# Optional: 1inch v6 API key (only needed if you enable 1inch fallback)
ONEINCH_API_KEY=

# Optional: Coingecko key (prices are using DefiLlama by default, no key required)
COINGECKO_API_KEY=
